# Phase 6 - 语音流式字幕与密集引用检查点

- 状态：IN_PROGRESS
- 范围：助手语音回答波形与逐分片字幕、RAG 句级密集引用、RAGFlow 文档名兼容和前端内联来源展示。
- 关键设计：前端按语音轮次保存 `audio_stream` 的 `sequence/text`，播放期间只显示已到达字幕，排空后恢复完整答案；RAG 合成要求每个可核查句子就近引用，稀疏首稿只基于同一批 chunk 修复一次；引用编号继续映射持久化 chunk 快照。
- 实际变更：助手气泡增加固定尺寸波形、播放状态和按序字幕；内联 `[n]` 改为可点击的“编号 + 文档名”；RAGFlow adapter 兼容 `document_keyword`、`docnm_kw`、`knowledgebase_id` 等字段；QueryService 增加句级引用检查和一次格式修复。
- 数据或接口兼容性：HTTP、SSE、WebSocket 和 SQLite schema 不变；只使用已有 `audio_stream.data.text` 字段；历史引用 JSON 保持原结构，文档名归一化覆盖更多 RAGFlow 版本字段。
- 验证命令与真实结果：Ruff 通过；Python unit 35 项、contract 4 项、定向 query/RAGFlow/audio 18 项通过；前端 lint、11 项 Vitest 和 production build 通过；`git diff --check` 通过。
- 外部服务验证：真实问题依次经过 LLM `need_rag`、RAGFlow 8 个 chunk 召回和 LLM 合成；最终 18 个可核查句均带有效引用，6 个引用快照均返回可展示文档名。详情见 `doc/05-testing/2026-09-02_dense-citation-smoke.md`。
- 提交：尚未提交；当前工作区包含用户此前的连续对话重构改动，按要求基于现状继续开发。
- 遗留问题：当前环境的 admin/audio `TestClient` 集成测试进入生命周期时等待；仓库无本地 Playwright，未新增截图结果；Phase 6 其他最终验收项仍未完成。
- 下一阶段入口：解决 TestClient 生命周期等待并补齐管理/语音浏览器 E2E，然后完成 Phase 6 最终验收。
