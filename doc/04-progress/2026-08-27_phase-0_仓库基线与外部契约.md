# Phase 0 - 仓库基线与外部契约

- 状态：DONE
- 范围：Python/React 工程、环境配置、脱敏日志、健康 API、RAGFlow 只读 adapter 和测试基线。
- 关键设计：外部响应只通过归一化模型进入业务层；secret 只从环境变量读取且不进入对象 repr；前后端均提供确定性测试命令。
- 实际变更：新增 FastAPI 工厂和 `/health`、Vite/React 壳、RAGFlow dataset/document/retrieval 只读接口、fixture、Ruff/pytest/Vitest/build 脚本。
- 数据或接口兼容性：无数据库变化；RAGFlow `code/data` 响应被 adapter 隔离，远端字段保存在不含鉴权信息的 `raw_metadata`。
- 验证命令与真实结果：`pytest tests/unit tests/contract tests/integration/test_health.py` 为 5 passed；Ruff 为 All checks passed；Vitest 为 1 passed；Vite production build 成功（193.59 kB JS，gzip 61.50 kB）。
- 外部服务验证：从本地忽略文件安全提取配置后，`GET /api/v1/datasets` live probe 成功；观察到 1 个现有 dataset、chunk method 为 `naive`，未创建或修改资源。
- 提交：`f2ff80d chore: bootstrap biosafe web assistant`；`c6ab4d2 feat(ragflow): add versioned API adapter contract`。
- 遗留问题：实例当前仅从已有数据观察到 `naive`；其他 method 将在 Phase 5 用 `biosafe-dev-` 资源逐一验证。LLM/ASR/TTS live 验证留到对应阶段。
- 下一阶段入口：建立单 SQLite 迁移和 repository，验证 84 行 Excel 两次导入。
