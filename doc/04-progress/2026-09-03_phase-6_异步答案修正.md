# Phase 6 - 异步答案修正检查点

- 状态：IN_PROGRESS
- 范围：把成功的 direct/RAG 问答非阻塞提交到有界队列，由独立强模型结合 web search 重新回答；在管理端展示任务状态、模型建议和公开来源，保留人工采纳与保存动作。
- 关键设计：`answer_correction` 与历史在同一 SQLite 中逻辑关联且不建外键；队列有容量与 worker 上限，支持关闭排空和重启恢复；模型建议不覆盖 `corrected_answer`，不进入 RAGFlow，不参与在线问答。配置优先使用 `CORRECTION_*`，兼容旧 `ANSWER_EVALUATION_*` / `EVAL_*`。
- 实际变更：新增修正配置、Responses API client、结构化输出/来源解析、任务 repository 和 dispatcher；QueryService 统一覆盖文本/语音；历史 API 附带模型修正并提供重试；管理端自动轮询、展示来源并将建议带入人工编辑框。
- 数据或接口兼容性：SQLite migration 2 新增 `answer_correction`；`HistoryItem.auto_correction` 是可空新增字段；新增 `POST /api/history/{id}/auto-correction`。旧历史、旧客户端和在线回答路径保持兼容。
- 验证命令与真实结果：后端定向 22 项通过；排除已知 TestClient portal 挂起的 admin/audio 5 项后，其余后端 52 项通过；Ruff 全量通过；前端 13 项测试、ESLint 和 production build 通过；`git diff --check` 通过。更新本机修正凭据后，隔离临时数据库的真实链路 `/api/chat -> QueryService -> answer_correction queue -> model -> /api/history` 返回 200，状态从 running 到 success，模型答案 25 字、公开来源 3 条，人工纠错字段保持为空。
- 外部服务验证：更新后的修正 API `/models` 返回 200，配置模型存在于 20 个可用模型中；Responses API + web search 单次调用和上述异步队列端到端调用均成功。旧失效凭据的 401 与有效凭据的成功结果均未输出或提交 secret。
- 提交：`8b749b2 feat(history): add asynchronous answer correction`。
- 遗留问题：当前环境中 admin/audio 的 Starlette `TestClient` 在 lifespan portal 启动时持续等待，属于此前已记录问题；浏览器人工检查仍需确认真实模型建议和三个来源链接的视觉展示。
- 下一阶段入口：完成管理端浏览器人工检查，随后解决 TestClient 生命周期并完成 Phase 6 其余全量 E2E。
