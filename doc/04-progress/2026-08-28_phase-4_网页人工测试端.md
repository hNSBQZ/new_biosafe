# Phase 4 - 网页人工测试端

- 状态：DONE
- 范围：
  - React + Vite 工作台承接助手第一屏、历史分页、纠错编辑和引用抽屉。
  - 文本问答继续消费 `POST /api/chat` SSE，历史读取 `/api/history`，实验读取 `/api/experiments`。
  - 录音入口只做浏览器控制和状态展示，不在前端模拟 ASR/TTS 成功。
- 关键设计：
  - `App.tsx` 里用 `useCallback` 稳定历史分页加载，用 `selectedHistoryIdRef` 避免分页刷新时读到过期选择。
  - 新增 `web/eslint.config.js`，把 React hooks 规则和浏览器全局变量显式放进前端 lint 配置。
  - 仓库当前没有 Playwright harness，本阶段不新建自动化脚手架，浏览器自动化留到后续阶段统一补齐。
- 实际变更：
  - `0941bdc feat(web): build text voice history and citation workspace`
  - `81b5c93 fix(web): stabilize history pagination load`
- 数据或接口兼容性：
  - 不改后端 schema，不改 SQLite 表。
  - 只消费已有 API 契约，没有引入本地检索、改写、重排或知识库写接口。
- 验证命令与真实结果：
  - `npm --prefix web run lint`：通过。
  - `npm --prefix web test -- --run`：3 passed。
  - `npm --prefix web run build`：通过。
  - `conda run -n biosafe python -m pytest tests/integration/test_chat_api.py tests/integration/test_history_api.py tests/integration/test_audio_api.py`：6 passed。
- 外部服务验证：
  - 本阶段不需要新的 live 外部服务调用，沿用既有后端契约和前一阶段的 RAGFlow/dev smoke 结果。
- 提交：
  - `81b5c93 fix(web): stabilize history pagination load`
  - `0941bdc feat(web): build text voice history and citation workspace`
- 遗留问题：
  - Playwright 自动化仍未引入；如需截图/关键流程回归，再单独补 Phase 6。
  - Phase 5 继续补 RAGFlow 知识库管理。
- 下一阶段入口：
  - 进入 Phase 5，补 dataset/document 管理、解析状态、重试、删除和检索预览。
