# Phase 6 - 前端路由与本地启动检查点

- 状态：IN_PROGRESS
- 范围：收敛人工演示页面、拆分知识库路由、完善语音录入与流式播放、提供 Python `.env` 启动入口和持久日志、补齐管理员与验收说明。
- 关键设计：前端使用 History API 提供四个稳定路由；助手按“实验场景 -> 文字/语音 -> 消息结果”组织；TTS 原始 PCM 分片显式携带格式/采样率/声道，浏览器按 sequence 缓冲并使用单个 AudioContext 连续调度；Python 启动脚本在导入 FastAPI 应用前加载 `.env`；应用和 Uvicorn 日志统一写 stderr 与滚动 JSON 文件。
- 实际变更：知识库从系统页移至 `/knowledge`；助手增加场景卡片与文字/语音分段控件；非助手页移除路径检查器；新增 `scripts/run_api.py` 和滚动文件 handler；管理员登录页和文档说明配置来源。后续修复 Vite `/api` 未开启 WebSocket 转发导致录音连接卡住的问题，增加 8 秒连接超时；录音按钮第二次点击改为停止并提交而不是取消；浏览器 WebM 录音在发送 ASR 前转换为 16kHz 单声道 WAV。PCM 固定使用 24000 Hz，增加事件元数据、失败分片 skipped 事件和前端有序播放队列；正常完成等待音频排空，取消/错误立即停止播放并保留文字答案。
- 数据或接口兼容性：现有 HTTP、SSE、SQLite 和 RAGFlow 接口不变；WebSocket `audio_stream` 只增加向后兼容的 `format/sample_rate/channels` 字段和 `skipped` 事件；新增浏览器路由要求生产部署支持 SPA fallback；管理员密码仍在启动时哈希更新。
- 验证命令与真实结果：后端全量 `pytest` 47 项通过；前端 lint、9 项测试和生产构建通过，其中播放器 5 项覆盖乱序、失败跳号、排空、取消和解码；`conda run -n biosafe python scripts/run_api.py --help` 与全量 ruff 通过；新脚本真实启动、健康和文件日志 smoke 通过；Playwright 桌面/移动布局无页面错误和横向溢出。语音录入通过 `10.158.0.31:5173` 完成 WS 握手并发送 16kHz WAV；真实外部 ASR -> RAG -> TTS smoke 返回 7 个音频分片且 `tts_success=true`；真实浏览器收到 24000Hz PCM 分片并连续调用 AudioBufferSourceNode.start，播放中取消调用 source.stop 且状态保持“已取消”。
- 外部服务验证：已真实调用配置的 ASR、LLM、RAGFlow retrieval 和 TTS，语音问题转写、RAG 回答、7 个 TTS 分片和正常终态均成功；浏览器流式播放与取消结果见 `doc/05-testing/2026-09-02_browser_streaming_audio_smoke.md`。
- 提交：`a0949fa feat(web): separate assistant and knowledge routes`；`4b0c40a chore: load dotenv and persist api logs`；`a33191b fix(voice): proxy websocket and submit wav audio`；`5acefb3 feat(voice): stream pcm audio in browser`。
- 遗留问题：Phase 6 的文本三路径 Playwright、知识库管理完整浏览器 E2E、生产部署配置和最终 secret/大文件审计尚未全部收口，因此不标 DONE。
- 下一阶段入口：补齐文本、历史和知识库浏览器 E2E，完成部署与最终验收报告。
