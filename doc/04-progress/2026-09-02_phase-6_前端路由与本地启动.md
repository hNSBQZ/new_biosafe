# Phase 6 - 前端路由与本地启动检查点

- 状态：IN_PROGRESS
- 范围：收敛人工演示页面、拆分知识库路由、提供 Python `.env` 启动入口和持久日志、补齐管理员与验收说明。
- 关键设计：前端使用 History API 提供四个稳定路由；助手按“实验场景 -> 文字/语音 -> 消息结果”组织；Python 启动脚本在导入 FastAPI 应用前加载 `.env`；应用和 Uvicorn 日志统一写 stderr 与滚动 JSON 文件。
- 实际变更：知识库从系统页移至 `/knowledge`；助手增加场景卡片与文字/语音分段控件；非助手页移除路径检查器；新增 `scripts/run_api.py`、`BIOSAFE_LOG_FILE` 和滚动文件 handler；管理员登录页和文档说明配置来源。后续修复 Vite `/api` 未开启 WebSocket 转发导致录音连接卡住的问题，增加 8 秒连接超时；录音按钮第二次点击改为停止并提交而不是取消；浏览器 WebM 录音在发送 ASR 前转换为 16kHz 单声道 WAV。
- 数据或接口兼容性：现有 HTTP、SSE、WebSocket、SQLite 和 RAGFlow 接口不变；新增浏览器路由要求生产部署支持 SPA fallback；管理员密码仍在启动时哈希更新。
- 验证命令与真实结果：后端全量 `pytest` 47 项通过；前端 lint、4 项测试和生产构建通过；`conda run -n biosafe python scripts/run_api.py --help` 通过；`conda run -n biosafe python -m ruff check biosafe services scripts tests` 通过；新脚本在 `127.0.0.1:18000` 真实启动，`GET /health` 返回 200，日志文件包含启动、access 和正常关闭记录，停止后端口无残留；Playwright 检查 1440x1000 和 390x844 的助手/知识库页面，页面错误为 0、无横向溢出，文字/语音切换可见，截图保存在 Git 忽略的 `logs/`。语音修复后 `ws://10.158.0.31:5173/api/v1/chat/audio` 握手返回 `connected`；虚拟麦克风浏览器烟测实际发送 `audio_start/audio_data/audio_end`，格式为 `wav`，音频头为 `RIFF...WAVE`，页面错误为 0。
- 外部服务验证：本检查点未调用 RAGFlow、LLM、ASR 或 TTS live 服务；Phase 6 最终 E2E 仍待执行。
- 提交：`a0949fa feat(web): separate assistant and knowledge routes`；`4b0c40a chore: load dotenv and persist api logs`；`a33191b fix(voice): proxy websocket and submit wav audio`。
- 遗留问题：运行后端 smoke、全量后端测试、浏览器桌面/移动截图与完整外部服务 E2E 尚未完成，Phase 6 不标 DONE。
- 下一阶段入口：按运维文档启动前后端，完成健康/日志 smoke、Playwright 路由与核心流程检查，再执行外部服务最终验收。
