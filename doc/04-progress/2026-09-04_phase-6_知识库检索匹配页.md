# Phase 6 工作段 - 知识库检索匹配页

- 状态：DONE
- 范围：
  - 排查语音问题“生物安全中的办公区是指什么？”未使用新上传法规 PDF 的原因。
  - 在知识库文件页增加“检索匹配”入口和独立 `/admin/knowledge/retrieval` 页面。
  - 新增管理员只读检索接口，展示在线默认配置下的有序 chunks、来源和分数。
- 关键设计：
  - `QueryService.default_retrieval_request()` 暴露在线 RAG 默认数据集、top-k、阈值和向量权重的快照；在线问答路径复用同一个内部构造方法。
  - 页面请求只包含原始问题，不能覆盖 dataset 或参数，避免诊断行为偏离线上默认配置。
  - 诊断接口不调用第一轮 LLM 或答案合成，不写历史，不修改 RAGFlow 资源。
  - dataset/chunk 技术细节只在管理员诊断页展示，知识库文件中心继续保持业务化视图。
- 实际变更：
  - 新增 `POST /api/admin/knowledge/retrieval-match` 及请求/响应 schema。
  - 响应返回默认 dataset 的 ID、名称和解析策略，以及 `page_size`、`similarity_threshold`、`vector_similarity_weight` 和有序 chunk。
  - 新增检索问题输入、默认配置摘要、固定排名、文档来源、综合/向量/关键词分数和 chunk 正文展示。
  - 增加桌面与移动端稳定布局，并复用现有管理员 token。
- 数据或接口兼容性：
  - 无 SQLite migration，无远端写操作。
  - 既有 `/api/admin/knowledge/retrieval-preview` 保持不变。
  - 未修改 `RAGFLOW_DATASET_IDS`；线上默认范围仍只有当前配置的通用资料库。
- 验证命令与真实结果：
  - `conda run --no-capture-output -n biosafe python -m pytest -q`：全量通过。
  - `conda run --no-capture-output -n biosafe python -m pytest tests/unit/test_query_service.py -q`：15 passed。
  - `conda run --no-capture-output -n biosafe python -m pytest tests/integration/test_admin_api.py -q`：8 passed。
  - `conda run --no-capture-output -n biosafe python -m ruff check biosafe services tests`：通过。
  - `npm --prefix web test -- --run`：2 个测试文件、15 tests passed。
  - `npm --prefix web run lint`：通过。
  - `npm --prefix web run build`：通过。
  - `git diff --check`：通过。
- 外部服务验证：
  - 历史记录 20 为 voice/RAG/completed，原始问题为“生物安全中的办公区是指什么？”，实际请求仅包含 dataset `9f08a224a2fb11f1adce556d4f780165`，top 8、阈值 0.2、向量权重 0.3。
  - 该默认 dataset 为 `biosafe-dev-p4-ppe-20260828161459`，`chunk_method=naive`；新 PDF 位于 `biosafe-dev-admin-20260829090945`，`chunk_method=laws`，状态 DONE、153 chunks。
  - 对法规库以原问题执行只读 top 50 检索，目标定义位于第 9，综合分 0.313772、向量分 0.866551、关键词分 0.076867；以“办公区 office area”查询时目标定义位于第 1。
  - 重启 API 后通过 Vite `5173` 代理调用新接口返回 HTTP 200，dataset、top 8、阈值 0.2、向量权重 0.3 与历史快照一致，并返回 8 个 chunks。
  - `GET /admin/knowledge/retrieval` 经 Vite SPA fallback 返回 HTTP 200；前端交互和渲染由 Vitest 确定性验证。
- 提交：`90602c6 feat(admin): add default retrieval match workspace`
- 遗留问题：
  - 默认检索范围尚未包含法规标准库；是否把五类业务库全部加入 `RAGFLOW_DATASET_IDS` 需要单独配置决策和真实召回验证。
  - 原问题在法规库排名第 9，当前 top 8 会截断目标定义；后续应基于代表性问题集评估 top-k 和 RAGFlow 分块/检索配置，不在本工作段临时调参。
  - 本机未安装 Playwright/Chromium，未生成浏览器截图；路由、交互、响应式 CSS 通过测试、build 和 HTTP smoke 验证。
- 下一阶段入口：
  - 使用检索匹配页对五类知识问题建立召回样本，再决定默认 dataset 范围和 top-k 调整。
