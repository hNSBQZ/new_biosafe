# 验收文档

## 当前结论

Phase 0 - Phase 5 已完成。本次已开始 Phase 6，完成前端演示页面收敛、知识库独立路由、Python `.env` 启动入口和文件日志；完整外部服务 E2E 与最终部署验收尚未完成，因此 Phase 6 保持 `IN_PROGRESS`。

## 首次准备

在仓库根目录安装项目依赖。`python-dotenv` 已作为正式依赖声明，不需要手工 `source .env`：

```bash
conda run -n biosafe python -m pip install -e .
```

确认根目录存在本机 `.env`。模板见 `.env.example`，真实密码、token 和 API key 只保存在 `.env`，不要写入文档或提交 Git。

## 启动后端

在仓库根目录运行：

```bash
conda run -n biosafe python scripts/run_api.py
```

脚本会由 Python 读取根目录 `.env`，进程中已经存在的环境变量优先。默认监听 `0.0.0.0:8000`，同时输出 Uvicorn access/error 日志和应用 JSON 日志。

常用参数：

```bash
conda run -n biosafe python scripts/run_api.py --help
conda run -n biosafe python scripts/run_api.py --host 127.0.0.1 --port 8000 --reload
conda run -n biosafe python scripts/run_api.py --env-file /path/to/local.env
```

后端健康地址：`http://localhost:8000/health`。

## 启动前端

另开终端，在仓库根目录运行：

```bash
npm --prefix web run dev
```

默认地址和页面：

| 地址 | 用途 |
| :--- | :--- |
| `http://localhost:5173/` | 助手演示：选择实验场景，切换文字或语音提问 |
| `http://localhost:5173/history` | 历史记录与人工纠错 |
| `http://localhost:5173/system` | 只读运行状态 |
| `http://localhost:5173/knowledge` | 管理员登录与知识库管理 |

## 管理员账号在哪里

管理员账号没有硬编码默认密码。它来自根目录 `.env`：

```dotenv
BIOSAFE_ADMIN_USERNAME=admin
BIOSAFE_ADMIN_PASSWORD=<本机管理员密码>
BIOSAFE_ADMIN_TOKEN_SECRET=<至少 32 字符的随机签名密钥>
BIOSAFE_ADMIN_TOKEN_TTL_SECONDS=28800
```

当前本机 `.env` 已检查到用户名、密码和签名密钥均已配置，但验收过程不会打印真实值。打开 `/knowledge` 后输入上述变量对应的用户名和密码。

需要修改密码时，修改 `.env` 中的 `BIOSAFE_ADMIN_PASSWORD` 并重启后端。应用启动时会把新密码的哈希更新到 SQLite，不保存明文。签名密钥可用 Python 生成：

```bash
conda run -n biosafe python -c "import secrets; print(secrets.token_urlsafe(48))"
```

若接口返回 `admin_auth_not_configured`，说明密码或签名密钥未配置；返回 `admin_login_failed` 表示用户名或密码不匹配。

## 日志在哪里

日志默认同时输出到当前终端和：

```text
logs/biosafe-api.log
```

日志文件达到 10 MiB 后滚动，保留 5 个备份；`logs/` 已被 Git 忽略。可在 `.env` 中调整：

```dotenv
BIOSAFE_LOG_LEVEL=INFO
BIOSAFE_LOG_FILE=logs/biosafe-api.log
```

日志使用 JSON 行格式，并对 Authorization、API key、token 和 password 脱敏。启动后至少应看到 `starting biosafe api`、Uvicorn 启动信息；访问健康接口后应看到 access 日志。

## 本次验收重点

1. `/` 能选择通用或具体实验场景，并且文字、语音输入不会同时堆在页面上。
2. 文字模式能发送问题、取消请求、查看回答路径和 RAGFlow 引用。
3. 语音模式能开始/停止录音，并保留转写、文字答案和 TTS 状态。
4. `/knowledge` 与演示页路由隔离，必须使用 `.env` 管理员账号登录。
5. 启动不需要 `set -a` 或 `source .env`，终端和日志文件都有真实运行日志。

更完整的运行和排障说明见 `doc/06-operations/2026-09-02_local-runbook.md`。
